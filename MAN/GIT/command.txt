#git help

show us status our project
#git status

address of repository
#git remote -v


git@github.com:tsaa1986/MyMoney.git


manage remote lists of repositaries
#git remote 

display all new changes
#git diff

#create branch
git branch develop

#switched develop branch
git checkout develop

#git commit -m "Add file index.html"

send changes to repositories
#git push
git push --all -u
git push -u origin <branch>/develop

get changes from repositories
#git pull


!!!
# start the ssh-agent in the background
$ eval "$(ssh-agent -s)"
> Agent pid 59566

#add private key
$ ssh-add ~/.ssh/id_ed25519


git clone git@github.com:tsaa1986/MyMoney.git